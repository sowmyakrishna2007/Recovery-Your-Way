import React from 'react';
import {
  StyleSheet,
  View,
  TextInput,
  ScrollView,
  TouchableOpacity,
} from 'react-native';

import {useNavigation} from '@react-navigation/native'

export default function recoveryFocus() {
    <ScrollView>
        <CustomText>Recovery Focus</CustomText>
    </ScrollView>
}






